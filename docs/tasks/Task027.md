# Task027 - Weekly AI Review

Version: V1.0
Status: TODO
Estimated Time: 2~3 hours

---

# Goal

Create weekly AI review generation.

After this task, the system must be able to summarize one week of recruiting logs and generate an AI-powered weekly review.

---

# Context

Task026 added logging and audit support.

Task027 extends AI Review from daily analysis to weekly analysis.

---

# Requirements

Create:

- Weekly review service
- Weekly review API
- Weekly review prompt
- Weekly review schema validation

---

# Files To Create or Modify

```text
src/services/weeklyReview.service.ts
src/app/api/review/weekly/generate/route.ts
src/app/api/review/weekly/route.ts
src/ai/schemas/weeklyReview.schema.ts
src/types/weeklyReview.ts
prompts/weekly-review.md
```

---

# API Endpoints

## Generate Weekly Review

```http
POST /api/review/weekly/generate
```

Request:

```json
{
  "startDate": "2026-01-01",
  "endDate": "2026-01-07"
}
```

---

# Weekly Review Output Schema

```json
{
  "summary": "string",
  "keyMetrics": "string",
  "strengths": "string",
  "weaknesses": "string",
  "suggestions": "string",
  "nextWeekFocus": "string",
  "score": 85
}
```

---

# Rules

- Must use RecruitLog data within date range
- Must not invent missing data
- Must calculate weekly KPI totals before AI call
- AI output must be valid JSON
- AI request must be logged through AIRequestLog

---

# Do NOT

Do NOT:

- implement monthly review
- create advanced charts
- add PDF export
- add authentication
- call AI from frontend

---

# Acceptance Criteria

- Weekly review can be generated by date range
- Weekly KPI data is included in AI input
- AI output is validated
- AI request is logged
- API response follows standard format
- Missing logs are handled clearly
- No TypeScript errors
- No lint errors

---

# Definition of Done

Task is complete when:

- `npm run build`
- `npm run lint`
- `npm run typecheck`
- `docker compose up --build`

all succeed.
